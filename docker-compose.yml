services:
  # PostgreSQL Database
  web_assessment_db:
    image: postgres:17-alpine
    container_name: web_assessment_db
    volumes:
      - postgres_data:/var/lib/postgresql/data
    env_file:
      - .env
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    networks:
      - web_assessment_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # Redis for Celery and Channels
  web_assessment_redis:
    image: redis:8.0.2-alpine
    container_name: web_assessment_redis
    volumes:
      - redis_data:/data
    networks:
      - web_assessment_network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # Django Web Application
  web_assessment_web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: web_assessment_web
    command: daphne -b 0.0.0.0 -p 8000 django_project.asgi:application
    volumes:
      - .:/app
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    ports:
      - "127.0.0.1:${WEB_PORT:-8000}:8000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@web_assessment_db:5432/${POSTGRES_DB}
      - CELERY_BROKER_URL=redis://web_assessment_redis:6379/0
      - CELERY_RESULT_BACKEND=redis://web_assessment_redis:6379/0
      - REDIS_HOST=web_assessment_redis
      - REDIS_PORT=6379
    networks:
      - web_assessment_network
    depends_on:
      web_assessment_db:
        condition: service_healthy
      web_assessment_redis:
        condition: service_healthy
    restart: unless-stopped

  # Celery Worker
  web_assessment_celery_worker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: web_assessment_celery_worker
    command: celery -A django_project worker -l info
    volumes:
      - .:/app
      - media_volume:/app/media
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@web_assessment_db:5432/${POSTGRES_DB}
      - CELERY_BROKER_URL=redis://web_assessment_redis:6379/0
      - CELERY_RESULT_BACKEND=redis://web_assessment_redis:6379/0
      - REDIS_HOST=web_assessment_redis
      - REDIS_PORT=6379
    networks:
      - web_assessment_network
    depends_on:
      web_assessment_db:
        condition: service_healthy
      web_assessment_redis:
        condition: service_healthy
    restart: unless-stopped

  # Celery Beat (for scheduled tasks)
  web_assessment_celery_beat:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: web_assessment_celery_beat
    command: celery -A django_project beat -l info
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@web_assessment_db:5432/${POSTGRES_DB}
      - CELERY_BROKER_URL=redis://web_assessment_redis:6379/0
      - CELERY_RESULT_BACKEND=redis://web_assessment_redis:6379/0
      - REDIS_HOST=web_assessment_redis
      - REDIS_PORT=6379
    networks:
      - web_assessment_network
    depends_on:
      web_assessment_db:
        condition: service_healthy
      web_assessment_redis:
        condition: service_healthy
    restart: unless-stopped

networks:
  web_assessment_network:
    external: true
    name: ${DOCKER_NETWORK_NAME}

volumes:
  postgres_data:
  redis_data:
  static_volume:
  media_volume: